import requests
import argparse

parser = argparse.ArgumentParser( description="Extract and analyze satellite imagery to study african deforestation" )
parser.add_argument( "--start_date" )
parser.add_argument( "--send_date" )
parser.add_argument( "--extent" ) 

args = parser.parse_args()


    

    

 

